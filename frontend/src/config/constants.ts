/* ═══════ PLANT CAPACITY CONSTANTS ═══════
 * Single source of truth for all tank/vessel capacities.
 * All values in KL (kilolitres) unless noted otherwise.
 */

export const PF_CAPACITY_KL = 450;        // Pre-Fermenter capacity (each)
export const FERM_CAPACITY_KL = 2300;      // Fermenter capacity (each, F1-F4)
export const BEER_WELL_CAPACITY_KL = 2300; // Beer Well capacity
export const RETENTION_HOURS = 12;         // Hours for retention countdown
export const FERMENTER_COUNT = 4;          // F1 through F4
export const PF_COUNT = 2;                 // PF1 and PF2
