node_modules/
dist/
frontend/public/
backend/public/
.env
*.db
*.db-journal
.DS_Store
*.log
uploads/
frontend/src/src/
uploads/
backend/ensure-server.sh
backend/keepalive.sh
backend/loadHistoricalData.ts
.claude/worktrees/

# Weighbridge runtime (local data and logs — not committed)
weighbridge/data/
weighbridge/logs/
graphify-out/

# Test artifacts
test-*.pdf
test-*.js
EINVOICE_IMPLEMENTATION.md
mobile/
generate_video.py

# Local tool scratch
.claude/scheduled_tasks.lock
.playwright-mcp/

# Vite build artifacts (hashed asset files rebuild on every deploy)
factory-server/public/assets/

# Python cache (sugar-opc/)
__pycache__/

# Vision ML project — local-only, never tracked.
# 2026-05-02: removed from origin via `git rm -r --cached vision/` after the
# initial push (commit 5666406) caused repeated "untracked files would be
# overwritten by checkout" pull conflicts on dev machines. ML weights, venv,
# data, and source all stay on the local filesystem only.
vision/
*.pt
*.pth

# Local DB backups (pg_dump output — never committed)
db-backups/
