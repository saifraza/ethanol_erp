{
  "name": "distillery-erp",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm ci && npx prisma generate && npx tsc --outDir dist && cp -r src/data dist/ && cd ../frontend && npm ci && npx vite build --outDir ../backend/public --emptyOutDir",
    "start": "cd backend && npx prisma db push --skip-generate && node dist/server.js",
    "postinstall": "npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.20.0"
  }
}
